#include "../include/Menu.hpp"
#include "../include/Equation.hpp"
#include <iostream>

using namespace std;

int main() {
    Menu m;
    m.menu();
    
    // a.bissection(1e-10);
    // a.display();
    // a.newton(1e-10);
    // a.display();

    return 0;
}